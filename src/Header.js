import React from 'react';

const Header = () => {
    return (
        <div>
            <ul>
            <a href="#">Home</a>
            <a href="#">About Us</a>
            <a href="#">Contact</a>
            <a href="#">Donate</a>
            </ul>
        </div>
    );
}

export default Header;